To run the database server, run the following command:
```
docker-compose up -d
```

Run the tests with the following command in the Tests directory:
```
dotnet test
```

To run the API, run the following command in the ProductManagerApi directory:
```
dotnet run
```
